apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: node-app-argo-application
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/satishnaga1/argocd-node-app-config.git
    targetRevision: main
    path: node-app2
  destination: 
    server: https://kubernetes.default.svc
    namespace: node-app2

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true
